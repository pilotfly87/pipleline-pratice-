pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'build app'
            }
        }

        stage('deploy') {
            steps {
                echo 'deploy app'
            }
        }

        stage('test') {
            steps {
                echo 'test app'
            }
        }

    }
}
